# Reviewed Linux baselines

Place human-reviewed PNGs here using the case names emitted by the harness:
`library.png`, `history.png`, `settings.png`, and `command.png`.

Do not copy screenshots into this directory automatically. Baselines are valid
only for the pinned Docker image, 1180x760 app window, and 96-DPI Xvfb display.

